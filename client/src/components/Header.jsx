// components/Header.jsx
import Navbar from './Navbar';
import Sidebar from './Sidebar';

const Header = () => {
    return (
        <header>
            <Navbar />
            <Sidebar />
        </header>
    );
};

export default Header;
